TELEGRAM_BOT_TOKEN=REPLACE_WITH_TOKEN
TARGET_CHAT_ID=REPLACE_WITH_TARGET_CHAT_ID
TZ=America/Argentina/Buenos_Aires
GROQ_API_KEY=REPLACE_WITH_GROQ_KEY

# Notes: create a real .env locally (never commit it). Use Railway/host env vars in production.